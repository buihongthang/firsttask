const initState = {
    user: [
        {id: 1, name: 'Thang'},
        {id: 2, name: 'Hoang'}
    ]
}

const rootReducer = (state = initState, actioc) => {

    return state;
}

export default rootReducer;